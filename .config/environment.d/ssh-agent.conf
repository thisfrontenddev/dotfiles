# Point all session processes at the systemd-managed ssh-agent socket.
# Read by systemd --user at login; inherited by the graphical session and shells.
SSH_AUTH_SOCK=${XDG_RUNTIME_DIR}/ssh-agent.socket
